{
  "name": "bess-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Internal BESS site-analysis workbench for behind-the-meter battery storage deals",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "sample": "node scripts/make-sample-data.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "playwright": "^1.60.0",
    "xlsx": "^0.18.5"
  }
}
